{{- if .Values.createSecret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "cn-ecs-data-platform.fullname" . }}-secrets
type: Opaque
stringData:
{{- range $key, $value := .Values.secrets }}
{{- if $value }}
  {{ $key }}: {{ $value | quote }}
{{- end }}
{{- end }}
{{- end }}
