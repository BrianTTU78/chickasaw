{{- if .Values.createSecret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "cn-ecs-chokwaa-app.fullname" . }}-secrets
type: Opaque
stringData:
{{- range $key, $value := .Values.secrets }}
{{- if $value }}
  {{ $key }}: {{ $value | quote }}
{{- end }}
{{- end }}
{{- end }}
