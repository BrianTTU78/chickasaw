apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "cn-ecs-data-platform.fullname" . }}
spec:
  revisionHistoryLimit: 1
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "cn-ecs-data-platform.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "cn-ecs-data-platform.name" . }}
    spec:
      containers:
        - name: {{ include "cn-ecs-data-platform.name" . }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.service.targetPort }}
          envFrom:
            - configMapRef:
                name: {{ include "cn-ecs-data-platform.fullname" . }}-config
            - secretRef:
                name: {{ include "cn-ecs-data-platform.fullname" . }}-secrets
