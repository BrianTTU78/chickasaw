apiVersion: v1
kind: Service
metadata:
  name: {{ include "cn-ecs-identity-app.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "cn-ecs-identity-app.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
