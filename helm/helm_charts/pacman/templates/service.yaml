apiVersion: v1
kind: Service
metadata:
  name: {{ include "pacman.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "pacman.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}
      protocol: TCP