apiVersion: v1
kind: Secret
metadata:
  name: {{ include "formio.fullname" . }}-secrets
  labels:
    {{- include "formio.labels" . | nindent 4 }}
type: Opaque
stringData:
{{- range $key, $value := .Values.secrets }}
  {{ $key }}: {{ $value | quote }}
{{- end }}